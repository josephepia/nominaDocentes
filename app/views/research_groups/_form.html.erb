<%= form_with(model: research_group) do |form| %>
  <% if research_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(research_group.errors.count, "error") %> prohibited this research_group from being saved:</h2>

      <ul>
        <% research_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :researchtype_id, style: "display: block" %>
    <%= form.text_field :researchtype_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
